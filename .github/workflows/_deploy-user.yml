name: Deploy User

on:
  workflow_call:
    inputs:
      user-image:
        required: true
        type: string
      eks-cluster:
        required: true
        type: string
      rds-endpoint:
        required: true
        type: string
      rds-secret-arn:
        required: true
        type: string
      user-role:
        required: true
        type: string
      s3-bucket-name:
        required: true
        type: string

jobs:
  deploy:
    name: Deploy User
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Configurando kubectl
        run: aws eks update-kubeconfig --region us-east-1 --name ${{ inputs.eks-cluster }}

      - name: Verificando Imagem
        run: |
          aws ecr describe-images \
            --repository-name fiapx-user \
            --image-ids imageTag=$(echo "${{ inputs.user-image }}" | cut -d':' -f2) \
            --region us-east-1
          echo "Imagem verificada"

      - name: Obtendo senha do banco
        id: db-secret
        run: |
          SECRET_JSON=$(aws secretsmanager get-secret-value \
            --secret-id ${{ inputs.rds-secret-arn }} \
            --query SecretString --output text)
          DB_PASSWORD=$(echo $SECRET_JSON | jq -r '.password')
          echo "::add-mask::$DB_PASSWORD"
          echo "password=$DB_PASSWORD" >> $GITHUB_OUTPUT

      - name: Criando Secret do Banco
        run: |
          kubectl create secret generic fiap-x-user-db-secret \
            --from-literal=DB_PASSWORD="${{ steps.db-secret.outputs.password }}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Criando JWT Secret
        run: |
          kubectl create secret generic jwt-secret \
            --from-literal=JWT_SECRET="${{ secrets.JWT_SECRET }}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy User
        env:
          USER_IMAGE: ${{ inputs.user-image }}
          USER_DB_ENDPOINT: ${{ inputs.rds-endpoint }}
          USER_ROLE_ARN: ${{ inputs.user-role }}
          S3_BUCKET_NAME: ${{ inputs.s3-bucket-name }}
        run: envsubst < k8s/user-deployment.yaml | kubectl apply -f -

      - name: Deploy HPA
        run: kubectl apply -f k8s/hpa.yaml

      - name: Aguardando Rollout
        run: kubectl rollout status deployment/fiap-x-user --timeout=600s

      - name: Verificando Implantacao
        run: |
          echo "User implantado com sucesso"
          kubectl get pods -l app=fiap-x-user
          kubectl get svc fiap-x-user